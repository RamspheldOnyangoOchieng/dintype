/**
 * Feature flags to enable/disable specific parts of the application
 * without removing the underlying code.
 */
export const FEATURES = {
    // Disable Telegram integration from the UI
    ENABLE_TELEGRAM: false,
};
